/**
 * Demo/placeholder values only — flagged here so they're easy to find
 * and replace once real CourtPlay details are supplied. Nothing below
 * is presented to users as a verified fact; no real contact number is
 * invented for the demo.
 */

// TODO: replace with CourtPlay's real WhatsApp Business link once known.
export const WHATSAPP_HREF = "#";
